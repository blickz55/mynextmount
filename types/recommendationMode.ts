/**
 * Farm list recommendation style — see `docs/scoring-model.md`.
 * `efficient` is labeled **Farmable** in the UI (drops + vendors only).
 */
export type RecommendationMode = "efficient" | "balanced" | "rarest";
