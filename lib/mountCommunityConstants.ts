/** Max body length for mount listing comments (VARCHAR matches Prisma). */
export const MOUNT_COMMENT_MAX_LENGTH = 2000;

/** Max spell IDs per batch summary request. */
export const MOUNT_COMMUNITY_BATCH_MAX = 200;

/** Default page size for comment thread. */
export const MOUNT_COMMENTS_PAGE_SIZE = 40;
