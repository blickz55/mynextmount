/**
 * Epic I.2 — First focusable control; links to #main-content (see pages + layout).
 */
export function SkipToMainContent() {
  return (
    <a href="#main-content" className="skip-to-main">
      Skip to main content
    </a>
  );
}
